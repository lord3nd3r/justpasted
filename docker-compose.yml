version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - HTTP_PORT=3000
      - NODE_ENV=development
      - EMAIL_VERIFICATION_ENABLED=0
    restart: unless-stopped
